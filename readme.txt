en package .json antes estaba esta "tslib": "^2.8.1", se cambio por la "tslib": "^2.3.0"

si hay un error de tslib utilizar npm install tslib, ingresar al contenedor con el comando 
- docker exec -it nombre_contenedor bash
- npm install tslib
